import type { GameConfig } from '../module/type/user';

const config = {
    mainScene: 'attic',
    gameName: '阁楼迷踪',
    items: {
        Water: {
            name: '水',
        },
    },
} satisfies GameConfig;

export default config;
